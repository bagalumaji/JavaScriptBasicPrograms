//Add an element to an array.

const arr = [1, 2, 3, 4, 5];
console.log("before adding element of array ", arr);

// it will add at end of array
arr.push(6);
console.log("after adding element of array ", arr);

arr.unshift(7);
console.log("after adding element of array ", arr);
